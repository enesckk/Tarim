# Field Parameter Catalog (Phase 2.2H)

51 field parameters seeded. `verificationStatus=Draft`. No scientific class thresholds. **Enum options are not seeded** without verified institutional sources (`FieldParameterOption` table ready).

Categories: SOIL_PROFILE, DRAINAGE, COMPACTION, STONINESS, EROSION, SURFACE, WATER, IRRIGATION, LAND_USE, VEGETATION, INFRASTRUCTURE, MANAGEMENT.

Value types: NUMERIC, BOOLEAN, TEXT, ENUM, PERCENTAGE, DEPTH, CLASSIFICATION.

API: `GET/POST /api/field-parameters`, `GET .../code/{code}`, `PUT .../{id}`.
