// Minimal ScrollTrigger stub for Vite (cloned landing page only uses GSAP for animations)
export const ScrollTrigger = {
  create: (_options: Record<string, unknown>) => ({}),
  update: () => {},
};

export default ScrollTrigger;
